import React from 'react';
import { ExportForm } from '@/features/inventory';

export const metadata = {
  title: 'Tạo Phiếu Xuất Kho | Gooli Admin',
};

export default function AdminCreateExportPage() {
  return <ExportForm />;
}
